name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - networkx
  - numpy
  - matplotlib
  - seaborn
  - plotly

    # Pip-only installs
  #- pip:
  #  - codecov

