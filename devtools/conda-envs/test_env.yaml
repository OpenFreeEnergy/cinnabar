name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - networkx
  - numpy
  - matplotlib
  - seaborn
  - plotly
  - sklearn

    # Pip-only installs
  #- pip:
  #  - codecov

